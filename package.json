{
  "name": "followthrough-slack-agent",
  "version": "1.0.0",
  "private": true,
  "description": "A Slack accountability agent that detects commitments and closes the loop.",
  "type": "module",
  "main": "src/app.js",
  "scripts": {
    "dev": "node --watch src/app.js",
    "start": "node src/app.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --test",
    "render:architecture": "node scripts/render-architecture.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^2.11.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@slack/bolt": "^4.4.0",
    "chrono-node": "^2.8.0",
    "dotenv": "^17.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
